# Netlify-Konfiguration für den Vokabeltrainer
# Reines statisches HTML aus der Repo-Wurzel + Serverless-Function zum Eintragen.
[build]
  publish = "."
  command = ""
  functions = "netlify/functions"
